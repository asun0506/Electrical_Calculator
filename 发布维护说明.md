# 网页发布与维护说明

本项目使用 GitHub 保存版本，并由 Cloudflare Pages 自动发布。

## 日常维护流程

1. 在本地修改并打开 `index.html` 验证功能。
2. 查看本次修改，确认没有把密码、密钥或隐私数据放入项目。
3. 提交修改并推送到 GitHub 的 `main` 分支。
4. Cloudflare Pages 会自动发布新版本。
5. 在正式网址上复查导航、计算、图片和导入导出功能。

## Cloudflare Pages 首次配置

- Production branch：`main`
- Framework preset：None
- Build command：留空
- Build output directory：`.`
- Root directory：仓库根目录

项目入口为 `index.html`。原入口 `Electrical_Calculator.html` 保留为兼容跳转页，过去的本地打开习惯仍然有效。

## 数据和安全边界

- 估价项目保存在每位用户自己的浏览器中，不会自动上传。
- 用户应使用 JSON 导出功能定期备份数据。
- 发布目录中的源代码、公式和基础数据库都可以被访问者查看。
- 禁止在 HTML、JavaScript 或其他发布文件中存放密码、访问令牌和商业机密。

## 回退版本

Cloudflare Pages 会保留部署记录；出现问题时可在部署页面回退。代码仍应在 Git 中修复并重新提交，保证线上版本与仓库一致。
