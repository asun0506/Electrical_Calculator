# 代码审查与架构说明（v1.4.0）

## 当前架构

工具采用无后端、无构建步骤的静态模块架构：

- `Electrical_Calculator.html`：唯一入口与脚本装配顺序。
- `js/app.js`：模块注册、导航、当前模块渲染及公共数值工具。
- `js/calc-*.js`：各子计算器，使用 `ElectricalToolkit.register()` 注册。
- `js/chart.js`：本地 SVG 图表渲染。
- `js/calc-iec60664-system.js`：三级绝缘距离校核、RSS 尺寸链、截图与报告导出。
- `js/materials-data.js`、`js/estimator-data.js`：只读基础数据库。
- `css/styles.css`：公共组件与各模块样式。
- 浏览器 `localStorage`：保存材料库用户覆盖、用户新增材料、上次打开模块和零件估价项目。

该结构适合本地双击运行，也便于其他 AI 助手直接编辑，不需要安装 Node.js 或启动服务器。

## 本次发现并修复的问题

1. 模块注册原先不检查重复 ID，可能造成导航项冲突；现在注册时会阻止重复 ID。
2. 原先任一模块渲染报错会使内容区直接空白；现在增加模块级错误隔离与可读错误提示。
3. 刷新页面始终回到第一个模块；现在会恢复上次打开的子计算器。
4. 保护配合模块的导入/导出箭头方向与全局规则相反；已统一为导入向下、导出向上。
5. 零件估价项目导入校验偏弱；现在会补齐基础字段、数组和明细行 ID，减少旧数据或不完整 JSON 导致的异常。
6. 零件估价成本原先允许负价格、负用量、超过 100% 的利用率/折价率/良率参与计算；现在统一限制到合理范围。
7. 估价模块同时监听 `input` 和 `change`，部分控件会重复触发保存和计算；现在按控件类型只绑定一种事件。
8. 估价 JSON 下载链接原先未插入文档，个别浏览器可能拒绝下载；现在采用兼容性更好的插入、点击、移除流程。
9. 材料库自定义名称进入下拉菜单时未完整转义；现在统一进行 HTML 转义。
10. 材料参数覆盖原先以字符串保存，可能造成后续数值处理不一致；现在按数值类型保存，并在本地存储失败时明确提示。

## UI 调整原则

- 用本地内嵌的单色线性 SVG 小图标替代彩色 Emoji 和数字序号。
- 深蓝灰主色配少量琥珀色强调，降低娱乐化和高饱和感。
- 圆角从大圆角收敛到 2–5 px，阴影改为极弱层级阴影。
- 输入、按钮、结果卡片采用工程软件常见的清晰边界。
- 状态依靠文字、色条和边框表达，不依赖表情符号。
- 保留黄色零件基础信息字段，延续估价工具的业务语义。

## 有意保留的设计

- 各计算器仍使用独立 DOM ID。由于页面一次只渲染一个模块，不会发生跨模块 ID 冲突；贸然重写为组件框架会显著增加离线维护成本。
- 未引入 npm、打包器或线上 CDN，确保解压后可直接双击使用。
- 未改变各子计算器的核心公式、输入参数和输出结果。

## v1.3.1 导航与模块精简

- 取消“保护与匹配”独立分组，将“继电器 / 保险丝匹配”归入“电气计算”。
- 移除功能过于简单的“塑料件估价”入口及源码模块。
- 图标全部随源码离线保存，不调用网络字体或在线图标库。

## v1.4.0 绝缘距离系统校核

- 原 IEC 60664 单点查询保留为标准数据内核，系统模块以显式 `replace` 注册机制替换导航呈现，不产生重复入口。
- 电芯、模组、PACK 三层边界独立计算并汇总，未填写完整的尺寸链不会被判定为通过。
- 尺寸链以 RSS 的 ±3σ 范围作为正式最小尺寸判定，极值累积只显示为风险参考。
- 用户截图在浏览器端缩放压缩，不上传服务器；报告由浏览器本地生成。
