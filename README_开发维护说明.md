# 本地综合计算器开发维护说明

## v1.9.2：连接器 LV / USCAR 体系扩充

- 标准库的“高低压连接器”新增德国LV体系：LV 214主规范、试验矩阵/慢动作/试验序列、LV 215高压要求与试验，以及LV 112-3、LV 216-1/-2配套导线规范。
- 新增北美USCAR/EWCAP体系：USCAR-2、12、17、18、21、25、26、30、37、38、44、45、49、53，覆盖低压、高压、压接、接地、RF/同轴、USB、焊接和线束固定。
- 页面明确区分产业/OEM联合规范与国家/国际标准；LV项目需取得目标OEM企业版，USCAR项目需核对受控规格及最新修订信。
- 数据回归测试：`tests/standards-data.cjs`；浏览器筛选回归同步更新于`tests/standards-library.cjs`。

## v1.9.1：RMS / 线径 / 汇流排温升整合

- 原“RMS 电流 / 线径估算”已整合到“RMS 电流 / 线径 / 汇流排温升”：同一份电流–时间 Map 同时输出 RMS、平均值、峰值、适用铜/铝线径和汇流排瞬态温升。
- 页面分别绘制电流–时间曲线与汇流排温升曲线，保留 AWG 静态对照表；载流密度可按项目条件调整。
- RMS 计算按 Map 实际时间跨度积分，不再默认起始时间为 0；无汇流排历史草稿时会尝试带入原 RMS 模块草稿。
- 回归测试：`tests/busbar-merged.cjs` 覆盖非零起始时间的 RMS / 平均值、峰值、线径向上取整及旧独立模块停用。

## v1.9.0：动力电池与车辆电气标准库

- 新增“工程知识库 / 动力电池与电气标准库”，覆盖动力电池、换电系统、电气架构、高低压连接器四个主题。
- 收录GB、ISO、IEC、UNECE的标准题录、适用层级、工程摘要、检索标签及发布机构官网入口；不复制受版权保护的标准正文。
- 支持全文搜索、主题/标准体系组合筛选、历史废止版本显隐及卡片/表格视图；筛选状态随浏览器草稿自动保留。
- 标准状态复核日期为2026-09-01。页面明确提示工程定版和认证前必须到发布机构官网再次核验，并区分产品标准、国际标准和车辆法规体系。
- 自动化测试：`tests/standards-library.cjs` 覆盖数据规模、搜索、组合筛选、历史版本、视图切换及官网域名约束。

## v1.8.2：英文报告导出

- 导体电阻、继电器/保险丝匹配增加英文 PDF；电气间隙/爬电距离增加英文 PDF 和 Word (.doc)。保持原有 A4 方向与中文导出入口。
- 零件估价支持项目/当前零件英文 Excel；材料数据库支持当前/全部材料英文 Excel。SOR 生成器保持原有双语导出。
- 英文版翻译固定标题、表头、判定结果、标准术语和图表标签；用户填写的名称、曲线名称、分析、备注及材料/估价原始数据文字保持原样。如需全文英文，请将这些内容填写为英文。图片、数字、单位和 JSON 格式不变。
- `js/report-language.js` 提供报告专用标签模板：仅翻译模板字面量，插值内容不自动翻译。不得对含用户内容的整份 HTML 进行全局替换。
- 回归测试：`tests/english-reports.cjs` 覆盖全部新增入口、中英文切换、数据/图片保留和 PDF 图例，实际 PDF 验证文件位于 `tmp/pdfs/`。

## v1.8.1：自动保存草稿与报告图例修复

- 各模块独立保留上次输入、动态表格、未完成曲线点、显示单位、图片和附件；切换、刷新、正常关闭后重新打开可继续编辑。
- 模块顶部显示保存状态，提供“恢复当前模块默认值”，确认后仅覆盖当前模块；材料库按钮只重置查询和未保存编辑，不删除已保存材料。
- 草稿保存在当前浏览器、当前站点的 IndexedDB，并尝试写入 localStorage 应急副本；大附件优先由 IndexedDB 容纳，保存失败会显示提示。不可替代 JSON 文件备份。
- 本地 file 页面、Cloudflare 域名、腾讯云域名及不同浏览器之间的草稿互不共享；切换使用环境请通过原有 JSON 导入导出迁移。清除站点数据、隐私模式或存储被浏览器回收可能导致草稿丢失，旧版本已经丢失的未保存输入无法追溯恢复。
- 继电器/保险丝报告图例改用 SVG 实线/虚线，关闭打印背景图形时仍显示；删除固定判读原则和外短提示段落，未填写人工分析时不输出占位文字。
- 新模块可提供 `captureDraft / restoreDraft / refreshDraft / resetDraft` 钩子，动态行和附件应使用数据模型恢复，不能用保存 HTML 代替事件绑定。
- 回归测试：`tests/drafts-and-report.cjs`，需要 Playwright（可用 `PLAYWRIGHT_MODULE` 指定安装路径）。自动使用临时浏览器配置，不触碰用户浏览器数据；PDF 校验输出位于 `tmp/pdfs/`。

## v1.8.0：电池包保护配合、AWG 与工程公式

- “继电器 / 保险丝匹配”前置电池包计算：支持电压、整包定值电阻，或由电芯/巴片/母排等多电阻段按尺寸或定值混合计算。
- 电阻段支持数量内串联/并联及 R1、R2…组合表达式，自动计算整包等效电阻和理想最大外短电流并绘入保护配合曲线。
- 移除原快速自动匹配与自动匹配结论；曲线下方改为用户人工填写校核分析，支持新版 JSON 和 A4 横向 PDF 校核报告。
- “RMS 电流 / 线径估算”新增 AWG 4/0～30 静态换算表，包含直径、截面积、20 °C 铜电阻及邻近公制规格。
- 新增“常见工程计算公式”，提供数学、力学、电学、热学四个子页面，包含符号、单位、适用提示，以及应力应变、Cu-Zn 和 Fe-Fe₃C 相图示意。

## v1.7.1：SOR 打印、自动序号与表格粘贴

- 修复 SOR 预览正常但浏览器打印/另存 PDF 为空白的问题；打印时将预览文档提升为页面顶层内容。
- 保持 Word 预览的逐页结构，每个预览页严格对应一张 A4，避免浏览器二次分页和末尾空白页。
- 自动识别“序号 / No.”列，初始化、增加行和删除行后按原有编号格式连续生成序号；`Pin Number` 不受影响。
- 表格单元格支持直接粘贴从 Excel 或其他表格复制的多行、多列内容，超出现有行数时自动增加行。

## v1.7.0：导体电阻项目校核

- 取消独立的单导体界面，单段和多段统一在导体段项目表中校核，项目允许只有一段导体。
- 每段支持 R1/R2 引用、自定义名称、材质、尺寸、温度、同规格数量及数量内串联/并联关系。
- 不同导体段继续支持 `+`、`//` 和括号表达式，可一键生成全部串联或全部并联。
- 每段可附加 PNG/JPG 图片；完整项目（含图片）支持 JSON 导入导出。
- 支持 Ω、mΩ、μΩ 显示单位，并可设置总电阻上限输出合格/不合格结论。
- 新增 A4 导体电阻校核报告，可通过浏览器打印或导出 PDF。

## v1.6.2：SOR 表格序号列自适应

- 自动识别“序号 / No.”等纯编号列并压缩为 78 px，释放的宽度由右侧内容列自动分配；`Pin Number` 保持普通列宽。
- 序号列标题和输入内容居中显示，操作列继续保持紧凑宽度。

## v1.6.1：SOR 章节化填写与原位附图

- 适配 2026-08-27 更新的公开版 `SOR Template.docx`，按模板正文顺序识别 34 个填写表格。
- 填写项按大章节归类、按章节号排序，并在每个文字项和表格上显示所属章节；章节标题本身不再被误识别为输入项。
- 所有表格统一为白底编辑样式，均可新增或删除行；新增行后保持当前章节展开。
- 模板中标有“（附图）”的单元格可原位上传 PNG/JPG，图片随 JSON 保存并嵌入生成的 Word/PDF 对应单元格。
- JSON 数据格式升级至 v3，仍兼容旧版没有原位图片数据的项目文件。

## v1.6.0：适配公开版表格化 SOR 模板

- 模板源更新为可公开的 `SOR Template.docx`，并提供 `scripts/build_sor_template.py` 进行可重复构建。
- 33 个标黄表格直接映射为表格式填写界面：14 个固定信息表、19 个支持增减行的清单表。
- 保留 23 个正文填写项、A4 页面、页眉页脚、目录字段、图片与原始样式；生成 Word 时自动请求更新目录。
- JSON、Word、PDF 预览与附件能力继续保留；封面 SOR 编号和版本自动同步到页眉。

## v1.5.2：SOR 封面信息合并

- 将模板解析产生的 19 个同名“文档封面与基本信息”卡片合并为一个集中章节。
- 合并后保留封面、项目、合作方、文档基本信息和响应/偏离说明表，一键填充会同时恢复这些内容。
- 目录条目不再显示为用户输入项；清空、JSON 导入和 Word 生成时会自动保留模板目录占位内容，打开 Word 后仍可更新整个目录。

## v1.5.1：绝缘距离多 Sigma 判定

- 尺寸链默认保持 RSS 3σ，每条关键尺寸可独立切换 RSS 4σ、RSS 6σ 或极值法。
- 现有 RSS 合成值视为 3σ，由 `σ = RSS / 3` 派生 4σ 和 6σ；极值法仍按各公差贡献绝对值直接累加。
- 页面表格、公差带图、Word/PDF 报告均同时列出 3σ/4σ/6σ/极值参考，并以用户所选方法重算最小尺寸与结论。
- JSON 仍使用 `schemaVersion: 1`；旧项目缺少判定方式时自动补为 RSS 3σ。

## v1.5.0：SOR 文件生成器

- 新增基于原始 DOCX 版式的 SOR 文件生成器，按章节引导填写模板标黄字段。
- 目录不作为输入项；Word 打开时提示更新目录和页码。
- 14 个关键表格支持按原行样式添加/删除多行，并提供章节级模板默认值恢复。
- JSON 导入导出会保存全部字段、动态表格和附件；图片附件可插入 Word/PDF 附件页。
- Word 直接修改原模板包，保留 A4 纵向、公司页眉页脚、双语样式、图片和表格版式。
- PDF 使用本地 Word 预览渲染后按 A4 打印，无需上传文档。
- 电气间隙/爬电距离报告补充明确的 A4 横向毫米尺寸，Word 与 PDF 页面比例一致。

本工具是无后端、无构建步骤的静态 HTML 项目，可直接编辑后刷新浏览器验证。

## 入口与模块机制

- 网站入口：`index.html`
- 兼容入口：`Electrical_Calculator.html`（自动跳转至 `index.html`）
- 公共样式：`css/styles.css`
- 注册与导航框架：`js/app.js`
- 每个 `js/calc-*.js` 文件是独立子计算器，通过 `ElectricalToolkit.register()` 注册。
- 汽车零件估价模块：`js/calc-part-estimator.js`
- IEC 60664 基础标准数据与单层算法：`js/calc-iec60664.js`
- 电芯/模组/PACK 三级系统校核与报告：`js/calc-iec60664-system.js`
- 估价底库：`js/estimator-data.js`
- Excel 离线导出组件：`js/vendor/xlsx.min.js`
- 估价底表参考：`docs/超级估价模板_工序组合扩充版.xlsx`

## 修改估价底库

`js/estimator-data.js` 暴露只读基础数据 `window.PART_ESTIMATOR_DATA`，其中包含：

- `materials`：原材料
- `processes`：工序
- `packaging`：包装
- `processTemplates`：标准工艺路线模板

网页中对单价、数量、工时、折旧、人工、良率等参数的修改只会写入项目数据，不会回写底库。

## 数据兼容与离线使用

- 项目存储键：`electrical_toolkit_part_estimator_v1`
- JSON 数据结构版本：`schemaVersion: 1`
- 所有依赖、底库和样式均为相对路径，禁止改成网络 CDN，否则会破坏纯离线使用。
- 如新增子计算器，在入口 HTML 中加入对应脚本标签即可；脚本加载顺序也是同一分组内的导航显示顺序。

## 发布检查

1. 保证 HTML 引用的 CSS、JS、图片、文档均存在。
2. 用浏览器直接以 `file://` 打开入口 HTML。
3. 验证所有导航项能切换。
4. 验证估价模块的材料/工序/包材添加、模板载入、拖拽排序、清空确认、JSON/Excel 导入导出。
5. 打包时保留完整目录结构和第三方许可证。

## 网页发布

本项目可通过 GitHub 与 Cloudflare Pages 自动发布，配置和日常流程见 `发布维护说明.md`。

## v1.3 工程化调整

- 框架会拒绝重复的计算器 ID，避免导航和模块路由相互覆盖。
- 单个模块渲染异常时显示局部错误信息，不再使整个工作区空白。
- 浏览器会记住上次打开的子计算器。
- 汽车零件估价导入时会补齐缺失字段和明细行 ID；成本计算会限制负数、利用率、回收折价率和良率的有效范围。
- 全站采用低圆角、低阴影、深蓝灰工程主题；导航使用本地单色线性 SVG 图标，不依赖彩色 Emoji、网络字体或在线图标库。
- v1.3.1 起，“继电器 / 保险丝匹配”归入“电气计算”，“塑料件估价”模块已移除。

## v1.4.0 IEC 60664 系统校核

- 电气间隙/爬电距离模块扩展为电芯级、模组级、PACK 级三级独立边界校核。
- 每层支持动态添加关键尺寸、截图、电气间隙/爬电距离类别及合格判定。
- 尺寸公差支持直接上下偏差与多环尺寸链；本版当时默认使用 RSS 3σ，后续版本已扩展多 Sigma 与极值法可选判定。
- 轮廓度和位置度默认按总公差带折算为单侧 `t/2`，普通尺寸公差按 `±t`，其他形位公差按全值贡献，并允许自定义折算系数。
- 报告汇总项目参数、三级标准与所有校核结论，支持 Word `.doc` 下载和浏览器打印为 PDF。
- 系统校核项目存储键：`electrical_toolkit_iec60664_verification_v1`。

## v1.4.1 校核备份与紧凑公差图

- 增加完整项目 JSON 导入/导出，图片以 Data URL 包含在 JSON 中，导入时与三级参数、尺寸链一起恢复。
- 增加带二次确认的一键清空功能。
- 尺寸链输入与 RSS 3σ 公差图在宽屏下改为左右布局；窄屏自动恢复上下布局。
- JSON 数据继续使用 `schemaVersion: 1`，导入前检查结构和 30 MB 文件上限。

## v1.4.2 报告布局同步

- Word/PDF 报告中的尺寸链明细与 RSS 3σ 公差图改为左右分栏，与网页编辑区保持一致。
- 报告使用兼容 Word 的嵌套表格布局，避免 Word 不支持 CSS Grid 时退回上下排列。
- 缩小报告图表高度和页边距，并启用打印颜色保留，减少单条尺寸链占用的页数。
